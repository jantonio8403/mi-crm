<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Inventario — HBC Berriozabal</title>
  <style>
    * { box-sizing:border-box; margin:0; padding:0; }
    body { font-family:Arial,sans-serif; font-size:11px; color:#111; background:#fff; padding:20px; }
    .encabezado { display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:14px; padding-bottom:10px; border-bottom:2px solid #1a7a4a; }
    .encabezado-left h1 { font-size:15px; font-weight:700; color:#1a7a4a; }
    .encabezado-right { text-align:right; font-size:10px; color:#555; }
    .filtros-resumen { background:#f5f5f5; border:1px solid #ddd; border-radius:4px; padding:6px 10px; margin-bottom:12px; font-size:10px; color:#444; }
    table { width:100%; border-collapse:collapse; }
    th { background:#0b3320; color:#fff; padding:6px 8px; text-align:left; font-size:10px; font-weight:600; text-transform:uppercase; letter-spacing:.04em; }
    td { padding:5px 8px; border-bottom:1px solid #e5e5e5; vertical-align:top; }
    tr:nth-child(even) td { background:#f9f9f9; }
    .badge { display:inline-block; padding:2px 6px; border-radius:3px; font-size:9px; font-weight:600; }
    .pie { margin-top:16px; font-size:9px; color:#888; text-align:right; }
    @media print { body { padding:0; } @page { margin:15mm 12mm; size:landscape; } }
  </style>
</head>
<body>
<div class="encabezado">
  <div class="encabezado-left">
    <div style="display:flex;align-items:center;gap:8px;margin-bottom:3px">
      <div style="width:26px;height:26px;background:#1a7a4a;border-radius:5px;display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:900;color:#fff;flex-shrink:0">A</div>
      <span style="font-size:18px;font-weight:800;color:#1a7a4a;letter-spacing:2px">GORA</span>
      <span style="font-size:10px;color:#555;border-left:1px solid #ccc;padding-left:8px;line-height:1.3">Sistema de Gestión Administrativa<br>H.B.C. 12 Camas · Berriozábal, Chiapas</span>
    </div>
    <p style="font-size:10px;color:#888;margin-top:2px">Reporte de Inventario de Bienes</p>
  </div>
  <div class="encabezado-right">
    <p><strong>Generado:</strong> <%= generadoEn %></p>
    <p><strong>Por:</strong> <%= generadoPor %></p>
    <p><strong>Total:</strong> <%= bienes.length %> bien<%= bienes.length !== 1 ? 'es' : '' %></p>
  </div>
</div>

<%
  const etiquetas = [];
  if (filtros.categoria) etiquetas.push('Categoría: ' + CATEGORIAS[filtros.categoria]);
  if (filtros.estado)    etiquetas.push('Estado: ' + ESTADOS[filtros.estado]);
  if (filtros.condicion) etiquetas.push('Condición: ' + CONDICIONES[filtros.condicion].label);
  if (filtros.area) { const a = areas.find(x => x.id == filtros.area); if (a) etiquetas.push('Área: ' + a.nombre); }
  if (filtros.buscar)    etiquetas.push('Búsqueda: "' + filtros.buscar + '"');
%>
<div class="filtros-resumen">
  <strong>Filtros:</strong> <%= etiquetas.length ? etiquetas.join(' · ') : 'Ninguno (todos los bienes activos)' %>
</div>

<table>
  <thead>
    <tr>
      <th>N° Inventario</th><th>Nombre</th><th>Categoría</th>
      <th>Marca / Modelo</th><th>N° Serie</th><th>Área</th>
      <th>Estado</th><th>Condición</th><th>Último mant.</th>
    </tr>
  </thead>
  <tbody>
  <% if (bienes.length === 0) { %>
    <tr><td colspan="9" style="text-align:center;padding:20px;color:#888">Sin resultados</td></tr>
  <% } %>
  <% bienes.forEach(b => { %>
    <tr>
      <td style="font-weight:700;white-space:nowrap;font-size:10px"><%= b.numero_inventario %></td>
      <td><%= b.nombre %></td>
      <td style="font-size:10px"><%= CATEGORIAS[b.categoria] %></td>
      <td style="font-size:10px"><%= [b.marca, b.modelo].filter(Boolean).join(' / ') || '—' %></td>
      <td style="font-size:10px"><%= b.numero_serie || '—' %></td>
      <td style="font-size:10px"><%= b.area_nombre || '—' %></td>
      <td><span class="badge" style="background:#e5e7eb;color:#374151"><%= ESTADOS[b.estado] %></span></td>
      <td><span class="badge" style="background:<%= CONDICIONES[b.condicion].color %>22;color:<%= CONDICIONES[b.condicion].color %>"><%= CONDICIONES[b.condicion].label %></span></td>
      <td style="font-size:10px;white-space:nowrap"><%= b.fecha_ultimo_mant ? fmtFecha(b.fecha_ultimo_mant, false) : '—' %></td>
    </tr>
  <% }) %>
  </tbody>
</table>

<div class="pie">Sistema AGORA · HBC Berriozábal · <%= generadoEn %></div>
<script>window.addEventListener('load', function(){ window.print(); });</script>
</body></html>
